services:
  - type: web
    name: document-generator
    env: node
    rootDir: cloud-server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
